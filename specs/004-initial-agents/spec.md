# Feature Specification: Initial Agents & Personnel Screen Enhancements

**Feature Branch**: `004-initial-agents`  
**Created**: 2026-01-21  
**Status**: Draft  
**Input**: User description: "The game will now be updated so that the Player's Governing Organizations starts with an initial set of 10 Agents. The Agents will be ten randomly chosen people from the Player's starting zone. The personnel screen will also be update to be more fully featured. The layout will be improved for a more professional feel, and the Profile will be updated to surface more information about selected Agents. A name generator will also be added, so people have more realistic and varied names."

## Clarifications

### Session 2026-01-21

- Q: Which population should Agents be selected from and are there age/assignment constraints? → A: People in the Player's starting zone have no age field (all are treated as adults) and it must be impossible for those People to be Agents of any other Governing Organization at game start. Selection therefore only considers unassigned People in the starting zone.

- Q: Where do the Agent/Governing Organization/Zone data shapes come from and how should the Profile obtain display fields? → A: The `Agent`, `Governing Organization`, and `Zone` canonical data models are the ones in `frontend/src/models`. When viewing an Agent's Profile on the Personnel screen, the UI MUST obtain the Agent's actual `name`, `homeZone`/`originZone`, `skills`, `attributes`, etc., from the associated `Person` entity referenced by the `Agent` object in those canonical models.

- Q: Which canonical `Person` fields should the Profile read? → A: Use canonical `Person` fields: `id`, `fullName`, `homeZone`/`originZone`, `skills`, `attributes`, `biography`. The Profile MUST read these directly from the `Person` referenced by the `Agent`.

## User Scenarios & Testing _(mandatory)_

### User Story 1 - Start a new game (Priority: P1)

As a Player, when starting a new game, my Governing Organization should begin with an initial roster of 10 Agents drawn from people in the Player's starting zone.

**Why this priority**: This establishes the player's initial personnel and immediate gameplay choices; it is required for a functioning starting state.

**Independent Test**: Create a new game with a deterministic seed and verify the Player's Governing Organization contains exactly 10 Agents whose source zone equals the player's starting zone.

**Acceptance Scenarios**:

1. **Given** a new game start, **When** the world is initialized, **Then** the Player's Governing Organization contains 10 Agent records.
2. **Given** the same deterministic seed, **When** the game is re-created, **Then** the same set of Agents is produced (for repeatable test runs).
3. **Given** a non-deterministic run, **When** the world is initialized, **Then** the 10 Agents are selected randomly from the population of the starting zone (no Agent outside the zone).

---

### User Story 2 - View personnel screen (Priority: P1)

As a Player, I can open the Personnel screen to see the list of Agents, access a selected Agent's Profile, and perform basic actions (view details, sort/filter the list).

**Why this priority**: The personnel screen is the UI surface for Agents; it's needed to interact with and manage the initial roster.

**Independent Test**: With a Player who has Agents, open the Personnel screen and verify layout, agent list, and Profile contents match the requirements below.

**Acceptance Scenarios**:

1. **Given** a Player with Agents, **When** they open the Personnel screen, **Then** the screen displays a list of Agents (>=1) with name, role, and a selectable entry for Profile.
2. **Given** an Agent selected in the list, **When** the Player opens the Profile pane, **Then** additional fields (biography snippet, skills summary, origin zone) are visible.
3. **Given** many Agents, **When** Player sorts or filters by role/zone/name, **Then** the list updates accordingly and remains usable.

---

### User Story 3 - Name generation during world generation (Priority: P2)

When new People are created during world generation, they should have realistic and varied names generated by the system.

**Why this priority**: Name realism improves immersion but is secondary to correct initialization and UI access.

**Independent Test**: Run world generation (with deterministic and non-deterministic seeds) and validate names for People created during generation according to uniqueness and pattern metrics (see Success Criteria).

**Acceptance Scenarios**:

1. **Given** world generation is creating People, **When** the name generator is invoked, **Then** each created Person receives a full name (given + family) that is non-empty and culturally plausible.
2. **Given** multiple world-generation runs, **When** 1,000 People names are sampled, **Then** at least 95% are unique and match a simple name-pattern sanity check (letters, typical separators).

---

### Edge Cases

- When the starting zone population contains fewer than 10 eligible people, the system should select as many unique Agents as possible and mark remaining Agent slots as empty/unassigned.
- When random selection produces duplicate individuals (by ID), the system must retry selection until 10 unique Agents or until a bounded retry limit is reached.
- If an Agent's profile data is incomplete, the personnel screen should show fallback placeholders rather than crashing.

## Requirements _(mandatory)_

### Functional Requirements

- **FR-001**: On a new game, the Player's Governing Organization MUST be initialized with exactly 10 Agent slots; filled slots must reference unique Agent entities when available.
- **FR-002**: The 10 Agents MUST be selected from the Player's starting zone population (no Agents from other zones), chosen randomly unless deterministic seeding is used for testing.
- **FR-003**: The Personnel screen MUST display a list of the Player's Agents including at minimum: full name, role/title, and origin zone.
  **FR-004**: Selecting an Agent in the list MUST open a Profile pane that displays additional details sourced from the associated `Person` record: biography snippet, skills summary, attributes, `homeZone`/origin, and any assigned roles. The Profile MUST read these fields via the canonical models in `frontend/src/models`.
- **FR-005**: The Personnel screen MUST support sorting by name, role, and origin zone, and simple filtering by role/zone.
- **FR-006**: The name generator MUST produce plausible full names (given + family) for People created during world generation and be callable during person/Agent creation and for display purposes.
- **FR-007**: When the starting population is insufficient to supply 10 unique Agents, the system MUST fill as many unique Agent slots as possible and expose remaining slots as empty (no silent failures).
- **FR-008**: People located in the Player's starting zone MUST NOT be assigned as Agents of any other Governing Organization at game initialization; they are considered unassigned for the purposes of initial Agent selection.

### Key Entities

**Agent**: Represents a person assignment record that references a `Person` entity from the canonical models. Key attributes: unique ID, reference to `Person` (source of `full name`, `homeZone`, `skills`, `attributes`), role/title, biography snippet. Note: People currently have no age field and are treated as adults.

- **Governing Organization**: Player-owned entity that has a roster of Agent slots (initially 10) and references to Agent entities; see canonical models in `frontend/src/models`.
- **Zone**: Geographical or logical area used as the selection pool for initial Agent assignment; canonical `Zone` model is in `frontend/src/models`.
- **NameGenerator**: Responsible for synthesizing realistic names given optional cultural/seed inputs; can be implemented as a service referenced by frontend or shared utility.

**Person (canonical model fields)**: The `Person` entity in `frontend/src/models` should expose at minimum the following fields for the Personnel Profile view:

- `id` — unique identifier for the Person
- `fullName` — the displayed full name (given + family)
- `homeZone` / `originZone` — reference to the Person's zone
- `skills` — summary or structured skills data used in Profile
- `attributes` — key attributes (traits, stats) relevant to the UI
- `biography` — short biography snippet for display

The Profile view MUST read these fields from the referenced `Person` rather than duplicating them on the `Agent` record.

## Success Criteria _(mandatory)_

### Measurable Outcomes

- **SC-001**: 100% of new game starts create a Governing Organization with exactly 10 Agent slots; at least 1 Agent slot is filled when the starting zone has >=1 eligible persons.
- **SC-002**: For deterministic test seeds, the same seed reproduces the same initial Agent set in 100% of runs.
- **SC-003**: When starting zone population >=10, at least 95% of new-game runs result in 10 unique Agents (no duplicates by ID).
- **SC-004**: Personnel screen primary view loads and becomes interactive within 1 second on a typical development machine (user-facing responsiveness target).
- **SC-005**: Name generator uniqueness: in a sample of 1,000 generated names, ≥95% are unique and ≥99% match sane name pattern (letters, spaces, typical punctuation).
- **SC-006**: UI correctness: the Personnel screen displays required fields (name, role, origin) for 100% of Agents without crashes; missing data shows placeholders.

## Constitution References \_(mandatory)

- See [.specify/memory/constitution.md](.specify/memory/constitution.md) for applicable principles. This feature follows the principles of predictable initialization (tests must be reproducible with seeds), user-facing UX consistency (personnel UI must be clear and accessible), and graceful degradation (fallbacks for missing Agent data).

**Spec links**: plan and task artifacts will be added to the feature directory: specs/004-initial-agents/

## Assumptions

- The game has an existing notion of Zones and a population of people that can be sampled for Agent creation.
- Deterministic seeding is available for tests that require reproducible Agent selection; if not available, tests use a mockable random source.
- Name generator will be limited to generating culturally-neutral full names unless additional localization requirements are requested.
- Performance target (1s) is a user-facing target for typical development hardware and will be validated empirically.

## Next Steps

- Implement Agent selection during new-game initialization and wire into the Player's Governing Organization.
- Implement the Personnel screen layout changes and Profile pane.
- Create or integrate a NameGenerator and add unit tests for uniqueness and format.
