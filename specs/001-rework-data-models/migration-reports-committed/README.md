This folder contains a small set of curated migration reports committed for documentation
and reviewer convenience.

- They are generated by the migration exporter (`scripts/migrate-fixtures.sh`) from the
  fixtures in `specs/001-rework-data-models/fixtures/`.
- These files are snapshots and should be updated intentionally when migration logic changes.

Provenance: generated by local run on branch `001-rework-data-models` and reviewed by maintainer.
